// ----------------------------------------------------------------------

const Routes = {
  // Common
  maintenance: "/maintenance",
  // Others
  //componentUI: (slug) => `/components-ui/${slug}`,
};

export default Routes;
